import "./globals.css";

export const metadata = {
  title: "Arc Genesis — AI Query Copilot",
  description: "Real-time AI-powered SQL safety platform. Predict and prevent production failures.",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <head>
        <link
          href="https://fonts.googleapis.com/css2?family=JetBrains+Mono:wght@400;500;600;700;800&family=Inter:wght@400;500;600;700&display=swap"
          rel="stylesheet"
        />
      </head>
      <body>{children}</body>
    </html>
  );
}
